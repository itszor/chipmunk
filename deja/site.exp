# The master configuration file should be here
# There is currently not a meaningful default configuration file

global target_list

case "$target_triplet" in {
  { "dop-*" } {
    set target_list "dop-sim"
  }
}

